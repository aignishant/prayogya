---
project: "P01"
day: 3
part: "3.2"
title: "The print that corrupted the protocol"
spine: 3
level: production
prerequisites: ["3.1 What crosses the line"]
prev: "3.1-what-crosses-the-line.md"
next: ""
prints: []
failure: true
---

## One-line answer

One `print` inside a tool put a line of English onto the wire, the client logged a parse failure —
and then answered the question correctly anyway, which is what makes this the worst kind of bug to
find later.

## The idea

The clerk at the hatch passes folders through, and the department has a convention: anything coming
through the hatch is a folder. Somebody, being helpful, starts slipping handwritten notes through
alongside them — *this one was already out this morning*, that kind of thing. Useful information,
and the first person on the receiving end drops it in the tray with the folders.

Nothing goes wrong for a while, because a note is obviously not a folder and people just put them
aside. Then the tray gets processed by somebody in a hurry, and a note ends up filed inside a claim.

On the stdio transport this is not an analogy. The protocol **is** the child process's standard
output: one JSON message per line, and every line is parsed as a message. A `print()` anywhere in
that process — in a tool, in a library, in an import — is a line on the wire.

The thing worth learning here is not "don't print". It is what the failure actually looks like,
because it is not a crash. The client cannot parse the line, says so somewhere you are probably not
reading, **skips it**, and carries on. Your call succeeds. The system works. And it will keep
working until the day a print lands in the middle of a message rather than between two of them, or
until it silently swallows something that mattered.

Day 1 already made the decision that prevents this: the logger writes to **standard error**. That
looked like tidiness at the time — keep the program's output free for its result. This part is why
it was load-bearing.

## The mechanism

One line added to `fetch_policy` in `claims_mcp/server.py`, of the kind anybody adds while
debugging:

```python
@server.tool()
def fetch_policy(number: str) -> dict[str, Any]:
    """Look up one policy by its number.
    ...
    """
    print(f"looking up {number}")  # broken on purpose: stdout is the protocol
    policy = store.policy(number)
```

**Line by line:**

- `print(f"looking up {number}")` — no logger, no prefix, no stream argument. It goes to standard
  output, which in this process is the wire.
- **The comment is in the printed code because this is a deliberate break**, and a reader who finds
  this file in that state should be able to tell in one line that it was on purpose.
- Everything else in the function is unchanged from part 2.1. **The failure needs no other
  ingredient**: one print, in one tool, in a process whose standard output belongs to the protocol.

Now call it across a real process line:

```bash
uv run python -c "
import asyncio
from mcp import Client, StdioServerParameters
async def main():
    params = StdioServerParameters(command='python', args=['-m', 'claims_mcp'])
    async with Client(params) as client:
        print('connected, protocol', client.protocol_version)
        r = await client.call_tool('fetch_policy', {'number': 'SYN-POL-1001'})
        print('result:', r.structured_content)
asyncio.run(main())
"
```

```
{"ts": "2026-09-10T11:35:54.891+00:00", "event": "boundary.fetch_policy", "number": "SYN-POL-1001", "found": true}
Failed to parse JSONRPC message from server
Traceback (most recent call last):
  File "...\.venv\Lib\site-packages\mcp\client\stdio.py", line 221, in _parse_line
    message = types.jsonrpc_message_adapter.validate_json(line, by_name=False)
              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  File "...\.venv\Lib\site-packages\pydantic\type_adapter.py", line 492, in validate_json
    return self.validator.validate_json(
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
pydantic_core._pydantic_core.ValidationError: 1 validation error for union[JSONRPCRequest,JSONRPCNotification,JSONRPCResponse,JSONRPCError]
  Invalid JSON: expected value at line 1 column 1 [type=json_invalid, input_value='looking up SYN-POL-1001\r', input_type=str]
    For further information visit https://errors.pydantic.dev/2.13/v/json_invalid
connected, protocol 2026-07-28
result: {'found': True, 'number': 'SYN-POL-1001', 'product': 'home-contents', 'status': 'active', 'inception': '2025-11-01', 'expiry': '2026-10-31', 'excess': 150, 'fast_track_limit': 2500, 'perils': ['accidental-damage', 'escape-of-water', 'fire', 'storm', 'theft']}
```

Verbatim, from this project, with the site-packages paths shortened.

**Line by line:**

- **The first line is the boundary's own log, on standard error**, and it is fine. It is JSON, it
  carries the event and the number, and it never went near the wire. This is day 1's decision
  working, in the same transcript as the failure it prevents.
- `Failed to parse JSONRPC message from server` — the client's own message, and note where it is:
  in the middle of the output, unlabelled, with no request id attached to it. In a real run this is
  one line in a scroll-back of thousands.
- `mcp\client\stdio.py, line 221, in _parse_line` — **the client is parsing a line**, singular. The
  stdio transport splits the child's standard output on newlines and validates each one, so the
  unit of corruption is a line.
- `input_value='looking up SYN-POL-1001\r'` — **there it is.** The exact string the `print`
  produced, quoted back in a pydantic validation error, with the carriage return Windows added.
  This is the most useful line in the whole traceback and it is the eighth line of it.
- `Invalid JSON: expected value at line 1 column 1` — the parser got as far as the first character.
  Nothing about this message says "somebody called print"; it says the wire has a malformed message
  on it, which is true and unhelpful.
- **`connected, protocol 2026-07-28` and the result print come after the traceback**, because they
  are the parent's own standard output and the child's standard error was already flowing. Reading
  this transcript top to bottom gives entirely the wrong impression of the order things happened
  in, which is its own small lesson about interleaved streams.
- **`result: {'found': True, ...}` — the call succeeded.** The correct policy, all fields, no error
  raised, `is_error` false. **The bad line was skipped and the protocol recovered**, because the
  stray text happened to land between two well-formed messages.
- So the state of the system is: one error logged where nobody is looking, one line of English
  discarded, and a correct answer. **A test asserting the result would pass.** That is the whole
  reason this part exists.

And it is not a thought experiment. With the print in place, the whole suite was run:

```
.........................                                                [100%]
25 passed in 8.30s
```

**Twenty-five passed.** Not one test in this project goes red — not the six boundary tests, not the
subprocess one, not the schema one. The only thing that changed is the number at the end: `8.30s`
against `1.66s` for the same suite the day before. The corruption is costing five times the
wall-clock and **the gate cannot see it**, because a gate reads an exit status and the exit status
is zero.

That is the honest shape of this bug: invisible to every assertion, visible only to somebody who
noticed the suite got slower and wondered why.

## When it breaks

The version above is the lucky one. What makes it dangerous is what it costs when the timing is
different — and the cost is asymmetric in a way worth stating plainly.

There are three outcomes from a stray line on this wire, and only the first is loud:

1. The line is skipped, the exchange continues, and everything looks fine — the transcript above.
2. The line arrives while the client is waiting for a specific response, the response never parses,
   and the call hangs until a timeout the client may not have set.
3. Something writes to standard output **without a trailing newline** — a progress indicator, a
   partial write — and the next real message is concatenated onto it. Now a valid message is
   corrupted, not merely accompanied.

The one that gets shipped is the first, because it does not look like a bug. Somebody adds the
print during an incident at eleven at night, the fix works, the print stays, and it sits in the
boundary for months writing a line per request into a stream the client is quietly failing to parse.

The smallest fix is to delete the print. The fix that survives the next person is the one this
project already made on day 1 and stated again in `claims_mcp/__main__.py`: **the boundary's only
output channel for anything human is standard error**, and that sentence is in the module docstring
of the file that runs the process, where somebody about to add a line will read it.

## In production

**What a senior writes instead.** They configure the logging framework before anything else runs,
with a handler that writes to standard error, and they treat any library that prints as a
dependency problem rather than a nuisance — because a third-party import that writes a
deprecation warning to standard output will do this to you and it will not be your code that shows
up in the traceback. On a long-lived service they move off stdio entirely: an HTTP transport has a
body per response and no shared stream to corrupt, which is day 4.

**What degrades at scale.** One stray line per request, on a desk processing several hundred
notifications a day, is several hundred parse failures a day that nobody sees — and the moment a
message is large enough to be written in more than one chunk, the odds of landing inside one rather
than between two stop being negligible. The failure then is not a skipped line; it is a claim
lookup that hangs, on one notification, intermittently, on a boundary that passes every test.

**The review comment.** *"There is a `print` in the MCP server. On stdio that is a message on the
wire — use `log()`, which goes to stderr. And while you are there: does anything we import print at
start-up? Run the module by hand and look at what comes out before the first request."*

**The interview question.** *"A server communicates over its standard output. A colleague adds a
debug print, the tests pass, and the feature ships. Describe what is actually happening on the
wire, and say what you would put in place so that the next person cannot do it."*

## Check yourself

- **Run:** the break, yourself. Add `print(f"looking up {number}")` to `fetch_policy`, run the
  round trip over a subprocess, and find `input_value='looking up SYN-POL-1001\r'` in the traceback.
  Then run `./run check` — **expect it to be green** — and compare the reported test duration with
  the run before. Take the print out.
- **Say out loud:** Twenty-five tests pass with the wire corrupted. Name the assertion you would
  have to add to catch it, say which of this project's existing tests is closest to being that
  assertion, and say why nobody writes it until after the first incident.

---

**This is the last part of day 3.** Tick the boxes in `CHECKLIST.md`, append the row to the ledger,
and commit. Day 4 gives the boundary its second transport — the server as a URL rather than a
process — and wires the desk to it as a client.
