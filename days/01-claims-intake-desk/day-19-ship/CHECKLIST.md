# P01 day 19 — definition of done

`python p.py done 01 19` refuses to commit while any box below is unticked. That refusal is the
point: a day is finished when it is understood and the checks are green, and by nothing else.

No box here carries a time estimate, and none ever will.

## Read

- [ ] `parts/01-the-container/1.1-stateless-container.md` — read · ran its check · answered its
      question out loud
- [ ] `parts/01-the-container/1.2-desk-as-a-service.md` — read · ran its check · answered its
      question out loud
- [ ] `parts/02-together/2.1-boundary-beside-the-desk.md` — read · ran its check · answered its
      question out loud
- [ ] `parts/03-the-pipeline/3.1-pipeline-that-runs-both-gates.md` — read · ran its check · answered
      its question out loud
- [ ] `parts/04-the-cold-clone/4.1-cold-clone-run-for-real.md` — read · ran its check · answered its
      question out loud
- [ ] `parts/04-the-cold-clone/4.2-strangers-entry-point.md` — read · ran its check · answered its
      question out loud
- [ ] `parts/05-holding-it/5.1-what-the-tests-hold.md` — read · ran its check · answered its
      question out loud

## Build

- [ ] `.dockerignore` — written **before** the `Dockerfile`, with `!.env.example` below `.env.*`
- [ ] `Dockerfile` — two stages, `python:3.12.12-slim`, `uv:0.12.3`, `--locked --no-dev`, `USER
      desk`, the `HEALTHCHECK`, exec-form `CMD`
- [ ] `claims_mcp/__main__.py` — the marked diff: `HOST` from `CLAIMS_MCP_HOST`, loopback by default
- [ ] `claims_desk/serve.py` — `HOST`, `PORT`, `NOBODY`, `innermost`, `health`, `Desk`, `main`
- [ ] `run` — the marked diff: the three docstring lines, `serve` and `up`, `PLANNED` emptied, the
      `FileNotFoundError` answer
- [ ] **Ran `./run serve` before writing compose**, on whatever port was free, and read all three
      routes
- [ ] `compose.yaml` — two services from one image, `CLAIMS_MCP_HOST: 0.0.0.0` on the boundary and
      no `ports:` on it, `env_file: .env` on the desk alone, `condition: service_healthy`
- [ ] `.github/workflows/check.yml` — `permissions: contents: read`, uv pinned to `0.12.3`, `uv sync
      --locked`, the key that is not a key, `./run check` then `./run eval`
- [ ] `.gitignore` — the marked diff: `data/claims/`
- [ ] `README.md` — six sections and the seventh, with the numbers your clone printed
- [ ] `CODEMAP.md` — generated by `python p.py codemap 01 --write`, never typed
- [ ] `tests/test_ship.py` — twelve tests, `in_process` **not** `autouse`, a server on port 0
- [ ] `Dockerfile` — `TODO(me)`: the three build commands on a machine with Docker, and the image's
      size in `PACKAGES.md`
- [ ] `Dockerfile` — `TODO(me)`: health separate from readiness, and what compose can do with the
      difference
- [ ] `claims_desk/serve.py` — `TODO(me)`: a shared secret on `POST /queue`, or the port
      unpublished, and the README agreeing
- [ ] `compose.yaml` — `TODO(me)`: `./run up` on a machine with Docker, with the queue's wall time
      beside day 17's
- [ ] `compose.yaml` — `TODO(me)`: a named volume for the claim files, or the README saying they
      are not persistent
- [ ] `.github/workflows/check.yml` — `TODO(me)`: the first run on the service, `gh run list` pasted
- [ ] `.github/workflows/check.yml` — `TODO(me)`: both actions by commit hash, with where you read
      each
- [ ] Your own clone — `TODO(me)`: the cold clone on Linux, its `uv sync --locked` beside part 4.1's
- [ ] `README.md` — `TODO(me)`: *what I would change*, in your own words

## Check

- [ ] The fast gate is green: `./run check` — `All checks passed!`, `292 passed`, four `ok` lines
- [ ] The slow gate is green: `./run eval` — twelve `ok`, `desk     12/12`, `eval: green`
- [ ] Ran `uv sync --locked --no-dev --dry-run` and read the five packages the image leaves out
- [ ] Ran `./run serve` and the three `curl` lines, then the dead-port variant, and read `503` with
      `ConnectError`
- [ ] Ran `./run up` and read either the compose run or `docker is not installed on this machine`
      with exit 127
- [ ] Ran both YAML parse blocks and read two services and seven steps
- [ ] **The cold clone, run for real.** An empty folder, an empty `UV_CACHE_DIR`, the README's
      commands, and what it printed pasted into part 4.1 in place of the author's transcripts
- [ ] **Break it on purpose, watch it go red, fix it.** The clone that forgot `uv.lock`: `Unable to
      find lockfile`. A pin moved without the lock: `needs to be updated`. `health()` returning
      `True, {"ok": True}`: three tests red, `assert 200 == 503`. `FROM python:3.12-slim`: the pin
      test red. `ports:` on the boundary: the count test red. Put every one back.
- [ ] **The failure that is green.** `uv sync` without `--locked` in the workflow, with a pin bumped
      and the lock untouched — said out loud what the runner would install, then put the flag back
- [ ] `python p.py depth 01 19` — green
- [ ] `python p.py check` — green across the whole repository

## Independence

- [ ] Every file this day printed was printed **in full**, at its real path — and the changes to
      earlier files are marked diffs naming the day of THIS project that printed each original
- [ ] Every concept this day used was **taught here**, at full depth — nothing discharged with a
      summary or a pointer
- [ ] Nothing in this day references another project — not a path, not a project name, and no
      sentence that assumes the reader has read one
- [ ] Nothing this day assumes was set up anywhere but in this project's own earlier days — and the
      cold clone is the proof
- [ ] No number in any part is quoted from this document rather than from your own run — the test
      count, the download sizes, the clone's timings, the process id on your port
- [ ] Nothing added today changes what the desk decides — twelve claims, four fast-tracked, seven
      recorded, one held, from `POST /queue` exactly as from `./run desk`
- [ ] No image, compose file or workflow carries a key, a key's value, or a file that could hold one

## Record

- [ ] Every term defined for the first time today has a row in `docs/GLOSSARY.md` — the nine rows
      in the hub §10
- [ ] The project's `PACKAGES.md` has the four rows from the hub §10, dated
- [ ] No `docs/PINS.md` row is owed — every version today is a project pin
- [ ] No `docs/SOURCES.md` row is owed — the eight reference pages are in the hub §9 with the date
- [ ] Noted honestly which transcripts are `TODO(me)` on this machine and why, and which day's
      machine will carry them
- [ ] The `docs/PROGRESS.md` row is pasted from the hub §10, inside the `granth:ledger` block
- [ ] Committed with the message from the hub §10 — the twentieth and last commit of this project
